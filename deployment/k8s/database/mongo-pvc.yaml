apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo
  namespace: default
  labels:
    app: mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi